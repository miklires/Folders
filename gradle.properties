org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# --------------------------------------------------------------------------
# Minecraft / toolchain versions.
#
# These MUST be checked against https://fabricmc.net/develop before the first
# build. They could not be resolved automatically because meta.fabricmc.net and
# maven.fabricmc.net are unreachable from the environment this was written in.
# Everything under src/main/java/dev/miklires/folders/core is independent of
# these values; only the integration + mixin layers care.
# --------------------------------------------------------------------------
minecraft_version=1.26.2
yarn_mappings=1.26.2+build.1
loader_version=0.17.2
fabric_version=0.135.0+1.26.2

mod_version=1.0.0
maven_group=dev.miklires
archives_base_name=folders
